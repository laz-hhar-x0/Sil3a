<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>📄 عرض السلع</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    body {
      font-family: sans-serif;
      background: #f4f4f4;
      margin: 0;
      direction: rtl;
    }

    .page-content {
      transform: translateX(-100%);
      opacity: 0;
      transition: transform 0.4s ease, opacity 0.4s ease;
      will-change: transform, opacity;
    }

    .page-content.show {
      transform: translateX(0);
      opacity: 1;
    }

    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px 16px;
      background-color: #1dce52;
      color: white;
      font-weight: bold;
      font-size: 18px;
    }

    .back-btn {
      background: transparent;
      color: white;
      border: none;
      font-size: 20px;
      cursor: pointer;
    }

    table {
      width: 100%;
      background: white;
      border-collapse: collapse;
      margin: 20px auto;
    }

    th, td {
      padding: 12px;
      text-align: center;
      border-bottom: 1px solid #ddd;
    }

    .options-container {
      position: relative;
      display: inline-block;
    }

    .options-btn {
      background: none;
      border: none;
      font-size: 18px;
      cursor: pointer;
    }

    .dropdown-menu {
      display: none;
      position: absolute;
      right: 0;
      top: 24px;
      background: white;
      box-shadow: 0 2px 8px rgba(0,0,0,0.2);
      border-radius: 5px;
      z-index: 10;
      min-width: 100px;
    }

    .dropdown-menu button {
      width: 100%;
      background: none;
      border: none;
      padding: 8px;
      cursor: pointer;
      text-align: right;
    }

    .dropdown-menu button:hover {
      background: #f0f0f0;
    }

    .dropdown-menu form {
      padding: 8px;
    }

    .dropdown-menu input[type="number"] {
      width: 60px;
      padding: 4px;
      margin-bottom: 5px;
    }

    .sell-btn {
      background-color: green;
      color: white;
      border: none;
      padding: 5px 10px;
      border-radius: 4px;
      cursor: pointer;
    }

    .sell-btn:hover {
      background-color: darkgreen;
    }

    #editModal {
      display: none;
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: rgba(0,0,0,0.5);
      justify-content: center;
      align-items: center;
      z-index: 1000;
    }

    #editModal .modal-content {
      background: white;
      padding: 20px;
      border-radius: 10px;
      width: 90%;
      max-width: 400px;
    }

    #editModal input {
      width: 100%;
      margin-bottom: 10px;
      padding: 10px;
      font-size: 16px;
    }

    #editModal button {
      padding: 10px;
      margin-left: 5px;
      font-size: 16px;
      border-radius: 5px;
      cursor: pointer;
    }

    #editModal .save-btn {
      background: #2196F3;
      color: white;
      border: none;
    }

    #editModal .cancel-btn {
      background: #ccc;
      border: none;
    }
  </style>
</head>
<body>
  <div class="page-content" id="content">
    <div class="navbar">
      <div> المبيعات</div>
      <button class="back-btn" id="backBtn"><i class="fas fa-arrow-left"></i></button>
    </div>

  <script>
    const content = document.getElementById("content");
    const backBtn = document.getElementById("backBtn");

    window.addEventListener("DOMContentLoaded", () => {
      requestAnimationFrame(() => {
        content.classList.add("show");
      });
    });

    backBtn.addEventListener("click", function(event) {
      event.preventDefault();
      content.classList.remove("show");
      content.classList.add("hide");
      setTimeout(() => {
        window.location.href = "/";
      }, 400);
    });

    const products = <%- JSON.stringify(products) %>;

    function toggleDropdown(event, index) {
      event.stopPropagation();
      document.querySelectorAll('.dropdown-menu').forEach(menu => {
        if (menu.id !== 'dropdown-' + index) {
          menu.style.display = 'none';
        }
      });
      const dropdown = document.getElementById('dropdown-' + index);
      dropdown.style.display = dropdown.style.display === 'block' ? 'none' : 'block';
    }

    document.addEventListener('click', () => {
      document.querySelectorAll('.dropdown-menu').forEach(menu => {
        menu.style.display = 'none';
      });
    });

    function handleEdit(index) {
      const product = products[index];
      openModal(product);
      document.getElementById('dropdown-' + index).style.display = 'none';
    }

    function handleSell(index) {
      const product = products[index];
      const dropdown = document.getElementById('dropdown-' + index);
      dropdown.innerHTML = `
        <form action="/sell/${product._id}" method="POST" onsubmit="return confirm('تأكيد عملية البيع؟')">
          <input type="number" name="soldQuantity" min="1" max="${product.quantity}" required placeholder="العدد">
          <button type="submit" class="sell-btn">💰</button>
        </form>
      `;
    }

    function openModal(product) {
      const modal = document.getElementById('editModal');
      document.getElementById('editName').value = product.name;
      document.getElementById('editQuantity').value = product.quantity;
      document.getElementById('editForm').action = `/edit/${product._id}`;
      modal.style.display = 'flex';
    }

    function closeModal() {
      document.getElementById('editModal').style.display = 'none';
    }
  </script>
</body>
</html>
